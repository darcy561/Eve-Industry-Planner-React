# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
frontend/node_modules
node_modules
/functions
/.pnp
.pnp.js
.vscode
.firebase
.tanstack
.vite
# testing
coverage/
.vitest/

# production
dist/

# Build output — never tracked, always rebuilt. `go build` in a package
# directory names the binary after that directory, so each main package has one
# possible output path; a new service needs a line here.
/eip
/deployment-tool/deployment-tool
/services/api/api
/services/capacity-controller/capacity-controller
/services/cmd/mongo_driver_v2_smoke/mongo_driver_v2_smoke
/services/cmd/mongo_parity_sample/mongo_parity_sample
/services/core/core
/services/core/scheduler/maintenance/maintenance
/services/websocket/websocket
/services/worker/worker
/services/worker/tasks/maintenance/maintenance
/services/ws-router/ws-router

# Go test binaries and profiles
*.test
*.prof
cover.out
coverage.out

# Editor and install backups
*~

# Generated by the TanStack Router plugin on dev, build and test runs.
routeTree.gen.js

# misc
*.exe
*.env
*.env*
services/worker/tmp/
mongo-keyfile*
bin/
docker-compose
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log
ui-debug.log
pubsub-debug.log
firestore-debug.log
database-debug.log
.firebaserc
adminSDK.json
adminSDKLive.json
/.tmp/
/temp/

# Firestore ArchivedJobs sample exports (see migration/README.md)
archivejobs_firestore_samples/
archivejobs_firestore_normalized/

# Sentry Config File
.env.sentry-build-plugin

# Operator config (local copy — make swarm-sync). Root only; keep kit embed template tracked.
/eip.config.yaml
.eip-sync.env
.eip-sync.env.bak
.eip-sync.*
.eip-swarm-secrets.yml
.eip-swarm-configs.yml
.eip-local-build.env
.eip-stack-mode

# Local backups (default .env stem backups/env/env; other tools may use backups/ later)
backups/
# Legacy root-stem .env backups
eip-env-backups/
eip-env-backup-current.txt
eip-env-backup-*.txt
*-env-backup-current.txt
*-env-backup-*.txt
