# Deprecated as a runtime plane. Swarm is the SoT:
#   docker-stack.data.yml  — mongo/redis/nats/seaweedfs/prometheus (eip-core)
#   docker-stack.obs.yml   — observability addon (eip-obs)
#   docker-stack.yml       — app train / edge
#
# Kept only so Public `update-files` / leftover cleanup still see a project name.
# `make up` / `make dev` do not bring Compose services up.

name: eve-industry-planner

services: {}
